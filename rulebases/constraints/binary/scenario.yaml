schema_version: 1
kind: groups
rules: rules.rules
facts: initial_facts.yaml
expected_facts: expected_facts.yaml
group_order:
  - propagate_binary_constraints
  - classify_csp_domains
  - classify_csp_problems
max_rounds: 3
